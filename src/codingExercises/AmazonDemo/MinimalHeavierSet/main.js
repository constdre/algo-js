import minimalHeavierSet from "./function";

const input = [5, 3, 1, 2, 4, 2];
console.log("input", input);
console.log("input length", input.length);
console.log("minimal heavier set", minimalHeavierSet(input));
